pipeline {
    agent any
    
    stages {
        stage('Pull Repository') {
            steps {
                // Checkout the repository
                checkout scm
            }
        }
    }
}
